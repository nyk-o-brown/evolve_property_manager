# Evolve Backend (Minimal PHP Skeleton)

Run locally:

1. Install dependencies with Composer
2. Serve `public/` via PHP built-in server

```
composer install
php -S localhost:8000 -t public
```

Environment variables in `.env` control DB and CORS.


